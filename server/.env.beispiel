# Aus dieser Datei liest "docker compose" die Werte, die nicht ins Repository
# gehoeren. Kopieren nach .env im selben Ordner und ausfuellen:
#
#   cp .env.beispiel .env
#
# .env selbst ist von der Versionierung ausgeschlossen.

# Fein abgestuftes Token, nur dieses Projekt, nur "Contents: Read-only".
# Anleitung: ../UPDATE.md -> Zugangsschluessel anlegen
TAGSTOCK_GITHUB_TOKEN=
